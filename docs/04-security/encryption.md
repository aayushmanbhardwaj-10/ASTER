# Encryption Philosophy
Data protection is achieved through encryption at rest and in transit using industry‑approved algorithms (AES‑256‑GCM for storage, TLS 1.3 with forward secrecy for network traffic). Encryption keys are managed by a dedicated, FIPS‑140‑2 validated Hardware Security Module (HSM) or cloud KMS, with automatic rotation every 90 days and strict separation of duties for key administration. Sensitive fields (e.g., PII, financial credentials) are additionally protected via field‑level encryption or tokenization where appropriate. All cryptographic primitives are vetted through regular third‑party audits, and we avoid custom crypto constructions. Backup and archive data inherit the same encryption standards, and key escrow is prohibited unless legally required and tightly governed.
