# Routes Directory

This directory contains application routing configuration.

Routes should be organized logically and lazy-loaded where appropriate for performance.
Each route definition should include path, component, and any required guards or metadata.
