# Contexts Directory

This directory contains React context providers for global state management.

Contexts should be used for truly global state that needs to be accessed by many components.
Each context should have a clear purpose and minimal API surface.
