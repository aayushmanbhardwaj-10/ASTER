# Assets Directory

This directory contains module-specific assets used across the application.

## Structure

- `icons/` - SVG icons (primary format)
- `images/` - Illustrations and photos
- `animations/` - Lottie files, CSS animations

Assets should be optimized for web use and organized by module when module-specific.
