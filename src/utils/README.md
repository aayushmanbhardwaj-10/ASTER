# Utils Directory

This directory contains cross-cutting utilities used throughout the application.

## Structure

- `formatters/` - Date, currency, number formatting utilities
- `validators/` - Input validation logic
- `helpers/` - General helper functions
- `constants/` - Application-wide constants

Utilities should be pure functions with no side effects and easily testable.
