# Hooks Directory

This directory contains custom React hooks (if applicable).

Custom hooks encapsulate reusable stateful logic and should follow React hooks rules.
Each hook should have a clear, descriptive name starting with "use".
